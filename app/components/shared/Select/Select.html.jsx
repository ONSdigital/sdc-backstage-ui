module.exports = function () {
	return (

		{/*<select className="select-dropdown" onChange={this.props.onChange}>
			{this.props.list.map(function (item) {
				return (
					<option key={item.value} value={item.value}>{item.title}</option>
				);
			}.bind(this))}
		</select>*/}

	);
};